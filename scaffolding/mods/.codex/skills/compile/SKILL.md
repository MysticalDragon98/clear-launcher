---
name: compile
description: Compile the Minecraft Fabric mod after applying requested code changes. Use when the user asks Codex to build, compile, verify compilation, or prepare the mod artifact for this repository.
---

# Compile

Read `MOD.md` before modifying or compiling the project.

Apply the uncommitted changes shown in MOD.md to the code while respecting the repository instructions.

Run:

```bash
mc-mods build
```

Report whether compilation finishes successfully. If it fails, summarize the relevant error and fix the problem when possible.

Then commit the changes.
